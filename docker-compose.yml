version: '3'

services:

    backend:
        build: ./backend
        container_name: backend
        
        ports:
            - 8080:8080
            
    frontend:
        build: ./frontend
        container_name: frontend
        ports:
            - 4200:4200
        links:
            - backend
            